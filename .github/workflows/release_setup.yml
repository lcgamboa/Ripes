name: Release setup

on:
  push:
    branches:
      - '*'

jobs:
  release-setup:
    runs-on: ubuntu-latest
    steps:

    - uses: "marvinpinto/action-automatic-releases@latest"
      if: github.ref == 'refs/heads/external_io_tcp_server'
      with:
        repo_token: "${{ secrets.GITHUB_TOKEN }}"
        automatic_release_tag: "continuoustest"
        prerelease: true
        title: "Continuous test release"
